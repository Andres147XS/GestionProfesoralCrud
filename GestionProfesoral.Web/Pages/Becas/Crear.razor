@page "/becas/crear"
@attribute [Authorize(Roles = "Administrador,Docente")]
@using GestionProfesoral.Shared.Models
@using GestionProfesoral.Web.Services
@inject ICrudService<Beca> BecaService
@inject NavigationManager Navigation

<h3>Nueva Beca</h3>

<EditForm Model="@beca" OnValidSubmit="GuardarBeca">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="mb-3">
        <label for="estudios" class="form-label">Estudios (ID)</label>
        <InputNumber id="estudios" class="form-control" @bind-Value="beca.Estudios" />
    </div>

    <div class="mb-3">
        <label for="tipo" class="form-label">Tipo</label>
        <InputText id="tipo" class="form-control" @bind-Value="beca.Tipo" />
    </div>

    <div class="mb-3">
        <label for="institucion" class="form-label">Institución</label>
        <InputText id="institucion" class="form-control" @bind-Value="beca.Institucion" />
    </div>

    <div class="mb-3">
        <label for="inicio" class="form-label">Fecha Inicio</label>
        <InputDate id="inicio" class="form-control" @bind-Value="beca.FechaInicio" />
    </div>

    <div class="mb-3">
        <label for="fin" class="form-label">Fecha Fin</label>
        <InputDate id="fin" class="form-control" @bind-Value="beca.FechaFin" />
    </div>

    <button type="submit" class="btn btn-primary">Guardar</button>
    <a href="/becas" class="btn btn-secondary">Cancelar</a>
</EditForm>

@code {
    private Beca beca = new Beca { FechaInicio = DateTime.Now, FechaFin = DateTime.Now };

    private async Task GuardarBeca()
    {
        if (await BecaService.CreateAsync("Beca", beca))
        {
            Navigation.NavigateTo("/becas");
        }
    }
}
